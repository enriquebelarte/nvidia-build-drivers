name: Run Script on File Change

on:
  push:
    paths:
      - 'matrix/drivers_matrix.MD5SUM'
  pull_request:
    paths:
      - 'matrix/drivers_matrix.MD5SUM' 

jobs:
  run_script:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout repository
        uses: actions/checkout@v2

      - name: Run script
        run: |
          sh ./matrix/create_matrix.sh
          sh ./matrix/update_dockerfile.sh
